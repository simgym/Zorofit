import React from "react";

const WorkoutDetails = ({ setWorkoutByUser }) => {
  let url = `https://api.api-ninjas.com/v1/exercises?type=${type}&muscle=${muscle}&difficulty=${difficulty}`;
};

export default WorkoutDetails;
